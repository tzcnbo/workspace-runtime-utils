# One-click Replit start

空 Replit workspace 里直接执行：

```bash
curl -fsSL https://raw.githubusercontent.com/tzcnbo/replit-openrouter-proxy/main/bootstrap-replit.sh | bash
```

如果已经在仓库目录里：

```bash
bash ./start-replit.sh
```

默认 API 端口：`8080`。
固定外部 API Key：`tzcnb`。
